# Bounded final source-fidelity debt sweep

Read-only sweep of the current v4 numbered statements, associated mathematical prose, paper-map declarations, and decisive theorem endpoints/producers. No additional mathematical debt was found beyond the two already assigned boundaries: Remark 4.10 integration and the arbitrary-universe ordinary-amalgamation form of Fact 2.6. This is a statement/hypothesis/producer sweep supported by the earlier independent proof reviews; it is not a new line-by-line proof audit of every previously frozen module, and it does not certify the parent's pending full gate or completion of those two boundaries.

## Coverage and significant checks

The map has 50 top-level entries: 47 numbered items, Proposition A, and two prose entries. I reread the corresponding mathematical source passages, including the finite free presentation before Lemma 2.31 and the global exponent-three convention beginning before Fact 2.15. The introduction, applications, and further-questions sections contain editorial placeholders rather than extra mathematical assertions.

- Notation 2.1: the new cardinal-valued minimum is attained for arbitrary groups and agrees with natural `Group.rank` under finite generation (`GeneratorRank/Cardinal.lean:35,47,98`). Thus `[FG]` in numerical bounds is the finite-number-of-generators hypothesis, not an unmentioned restriction on arbitrary groups. The exact commutator, right conjugation, ordinary coproduct universal property, and actual least normal closure are in `Notation.lean:33,41,54,70`.
- Definitions 2.2/2.4 and Facts 2.3/2.5: syntactic model completeness remains the definition (`ModelCompanion.lean:104`); the semantic reverse is actually proved (`ModelCompleteness.lean:457`). The model-companion characterization has both directions (`ModelCompanionCriterion.lean:43,65`). Companion embeddings extend to arbitrary source universes (`ModelEmbeddings.lean:100`), e.c. reflection is proved for arbitrary target universes (`ExistentialClosedness.lean:64`), and companion-model/e.c. equality holds there (`:128`). Local finiteness and the same finite cover/bound transfer without a Small hypothesis or a T-model assumption on finitely generated substructures (`LocallyFinite/Universes.lean:37,66`, `UniformLocalFiniteness/Universes.lean:38,87,113`). Finite diagrams really have finite T-equivalence-class representatives, not just a finite conjunction.
- Definitions 2.7–2.11: native LieRing/LieAlgebra/GradedLieAlgebra and positivity are used, with the cyclic-Jacobi constructor and the full degree-one-generation equivalence (`GradedLie.lean:77,150,263`). The zero-degree condition expresses the paper's positive grading. The block-homogeneity criterion and quotient use an actual DirectSum decomposition, arbitrary indices, and the componentwise representative formula (`BlockDecomposition.lean:43,51,82,177,203,222`).
- Facts 2.14/2.15: general nilpotent central-series results do not assume exponent three (`CentralSeries.lean:65,103,137`). The extra `[Group.IsNilpotent]` in the nilpotency-class statement expresses its source hypothesis. The arbitrary-set product inclusions use pointwise products of actual commutator-generated subgroups (`Identities.lean:763,797`), and the nonabelian Z2 counterexample is supplied (`Free/Examples.lean:69`).
- Definition 2.18, Lemma 2.19, Notation 2.20: actual successive group quotients, induced quotient bracket, module structure, full graded Lie structure, Lie generation and full graded triple identities are supplied. The subgroup image uses the ambient filtration, with actual kernel and quotient equivalence (`AssociatedGraded.lean:390,398,461,483`), not the intrinsic filtration of the subgroup. Example 2.21 now includes graded structure and bracket, not dimensions alone (`AssociatedGradedExamples.lean:193,369,456,501,513,530,550`).
- Proposition 2.24 and Lemma 2.31: the injectivity/strictness iff retains the injective group-map assumption exactly where required (`AssociatedGraded/Lie.lean:324,332`). Quotient layers are the canonical quotient by the ambient graded image, for arbitrary normal subgroups (`GradedQuotient.lean:145,179,200`).
- Facts/Remarks 2.27–2.30: finite normal forms and exact cardinalities are proved, including the actual ascending word (`Free/NormalForm.lean:599,882`). Infinite-rank words have finitely supported unique coordinates (`Free/InfiniteNormalForm.lean:279`). The actual free-group graded Lie algebra maps to actual exterior powers for any specified basis and arbitrary rank, preserving all grades (`Free/ExteriorLie.lean:163,191`); a chosen order is not a finite-rank assumption.
- Propositions 3.1/3.2 and Theorem 3.3: conjugate width is at most three positive right conjugates (`ConjugateWidth.lean:197`). Support uses rank(B), not card(B), and returns a certificate in H0's actual internal normal closure (`Support.lean:216`). The logarithm chain is explicit (`GeneratorRank/Cardinality.lean:113,144`) and the exact witness function is fixed independently of the base/model. Both failed-amalgam factor cases are present (`Main/BoundedWitness.lean:112`).
- Proposition 4.1: the full prescribed-basis/prescribed-representative theorem is unconditional on those choices beyond the representative equations (`Presentation.lean:184`). The kernel-basis defects are actually produced (`Roots/DefectBasis.lean:46,114,135`), rather than left as assumed producers in the strictification endpoints.
- Proposition 4.3 received a fresh independent child sweep of TeX843–945 and the decisive kernel chain: canonical eta maps; `GradedEquiv.lean:34–110`; actual basis presentation; `FreeGraded.lean:332,359`; `Presentation.lean:407,506,544`; `Relations.lean:329`; `QuotientMaps.lean:163,180`; `GradedNormalClosure.lean:269,306`; `GradedQuotient.lean:103`. No finite-rank, order, homogeneous-relation, or conditional-bijectivity premise remains unpaid. The (1,2) mixed component is the actual bracket in the paper's order, with its exterior-coordinate minus sign explicit. The total Lie bracket satisfies the multigrading law for all Nat-pair indices (`BlockBracket.lean:270,342`).
- Lemmas 4.4/4.5: strict coproduct injectivity is for arbitrary factors (`Coproduct/Strict.lean:114`); the F2 stabilization uses exactly the source's Nontrivial G, with no finite-generation assumption (`Coproduct/CentralSeries.lean:26,44`).
- Lemmas 4.6–4.9: simultaneous coproduct/product quotient constructions and exact numerical bounds have actual terminal producers (`Roots/Commutator.lean:505`, `Roots/Triple.lean:269`, `Roots/DerivedStrictification.lean:306`, `Roots/LowerCentralStrictification.lean:300`). The degree-two and degree-three defect generators are chosen from actual quotient kernels. No root independence hypothesis is imposed on the prescribed group elements.
- Proposition A and Propositions 4.11/4.12: all three e.c. conclusions are present (`ExistentiallyClosedGroups.lean:279,308,323`). The strict envelope carries internal central-series coincidence as well as ambient strictness and exactly 15 n^2 (`StrictEnvelope.lean:129,169`). Its trivial-subgroup branch returns C itself, so n=0 does not invoke F2 stabilization or lose the bound. Finite generation is equivalent to finiteness for exponent-three groups; the source assumption is preserved. The full-C-fixed diagram and the recent arbitrary-universe generalizations have also received the universe lane's independent review. `Main/ModelCompanion.lean:122` assumes no supplied bound: the bound theorem is proved and applied.

## Known remaining boundaries

Remark 4.10 is still marked planned in the inspected map. The six actual F4/F3 quotient candidates have now passed independent fidelity review and scoped strict/environment/text/name/private-axiom checks, but their promotion/full gate are not asserted here. See `/tmp/t3-shared-roots-independent-review.md` and `/tmp/t3-shared-roots-audit/report.json`.

The existing Fact 2.6 iff and obstruction predicate use the canonical semantic universe. The independent universe lane is supplying the ordinary-amalgamation bridge and arbitrary-model obstruction form. This remains a known scope boundary even while its map entry says proved; it must be integrated before asserting the final all-universe source claim.

## Historical documents

The D1–D6 gaps in `notes/paper-faithfulness-audit.md:38,48,70,78,95,115` are an explicitly pinned audit of the old sibling repository, not live gaps in this implementation. All six have current exact producers except the separately identified final generic-universe bridge scope. `notes/implementation-plan.md:30–102` is a chronology ending at an earlier checkpoint; its old 'next' statements must not be used as a current proof inventory. `notes/bounded-amalgamation-implementation-frontier.md:9–12,235` still presents Fact 2.6 as future work but identifies itself as the preimplementation design record; the canonical theorem now exists. The latest `notes/semantic-universes-and-notation-checkpoint.md:38–43` accurately lists the two remaining boundaries. A final live summary should point to the final checkpoint, leaving historical audit snapshots intact.

No project files were edited and no global gate was rerun by this sweep.

Source SHA256: `79745dfa1660a827c51ec3c6b2006ee9d6243cb4702f97a75e5b55357b89f50b`.
Inspected map SHA256: `4c01ebfdacd8c63cf64ab993001eec4814ed5d6e607cd70f6a7e72fc865775df`.
